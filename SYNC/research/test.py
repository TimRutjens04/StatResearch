from gym import *
import random

# machine_types = list(MachineTypes)

# random_workout = random.sample(machine_types, k=random.randint(2, 5))

# print("Random machines:", [machine.name for machine in random_workout])


